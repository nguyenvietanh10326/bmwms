@page
@model BMWMS.Web.Pages.Transfer.IndexModel
@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Äiá»u chuyá»ƒn kho";
    ViewData["ActivePage"] = "Transfer";
}

<div class="transfer-page">
    <div class="pagehead" style="display:flex; justify-content:space-between; align-items:flex-end;">
        <div>
            <h1>Danh sÃ¡ch Lá»‡nh Äiá»u Chuyá»ƒn</h1>
            <p>Quáº£n lÃ½ cÃ¡c lá»‡nh Ä‘iá»u chuyá»ƒn ná»™i bá»™.</p>
        </div>
        <div>
            <a href="/Transfer/Create" class="btn btn-primary"><i class="ri-add-line"></i> Táº¡o phiáº¿u má»›i</a>
        </div>
    </div>

    @if (!string.IsNullOrEmpty(Model.SuccessMessage)) { <div class="alert alert-success">@Model.SuccessMessage</div> }
    @if (!string.IsNullOrEmpty(Model.ErrorMessage)) { <div class="alert alert-danger">@Model.ErrorMessage</div> }

    <div class="card" style="padding: 20px; background: #fff; border-radius: 12px; border: 1px solid #e2e8f0; margin-bottom: 20px;">
        <form method="get" class="d-flex gap-2">
            <input type="text" name="Keyword" value="@Model.Keyword" placeholder="Nháº­p mÃ£ phiáº¿u..." class="form-control" style="max-width:300px;" />
            <select name="Status" class="form-select" style="max-width:200px;">
                <option value="">-- Táº¥t cáº£ tráº¡ng thÃ¡i --</option>
                <option value="DRAFT" selected="@(Model.Status == "DRAFT" ? "selected" : null)">NhÃ¡p (DRAFT)</option>
                <option value="APPROVED" selected="@(Model.Status == "APPROVED" ? "selected" : null)">ÄÃ£ duyá»‡t (APPROVED)</option>
                <option value="COMPLETED" selected="@(Model.Status == "COMPLETED" ? "selected" : null)">HoÃ n thÃ nh (COMPLETED)</option>
                <option value="CANCELLED" selected="@(Model.Status == "CANCELLED" ? "selected" : null)">ÄÃ£ há»§y (CANCELLED)</option>
            </select>
            <button type="submit" class="btn btn-dark">Lá»c</button>
            <a href="/Transfer/Index" class="btn btn-light">XÃ³a lá»c</a>
        </form>
    </div>

    <div class="card" style="background:#fff; border:1px solid #e2e8f0; border-radius:12px; overflow:hidden;">
        <table class="table table-hover mb-0" style="font-size:13px;">
            <thead style="background:#f8fafc;">
                <tr>
                    <th>MÃ£ Phiáº¿u</th>
                    <th>NgÃ y yÃªu cáº§u</th>
                    <th>NgÆ°á»i táº¡o</th>
                    <th>NgÆ°á»i thá»±c hiá»‡n</th>
                    <th>Tá»•ng S.L</th>
                    <th>Tráº¡ng ThÃ¡i</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if(Model.PagedResult.Items.Any())
                {
                    @foreach(var item in Model.PagedResult.Items)
                    {
                        <tr>
                            <td style="font-weight:600;"><a href="/Transfer/Details?id=@item.TransferOrderId">@item.TransferOrderNumber</a></td>
                            <td>@item.RequestedDate.ToString("dd/MM/yyyy")</td>
                            <td>@item.CreatedByName</td>
                            <td>@item.AssignedToName</td>
                            <td>@item.TotalRequestedQuantity</td>
                            <td><span class="badge bg-@item.StatusCss">@item.StatusLabel</span></td>
                            <td class="text-end">
                                <a href="/Transfer/Details?id=@item.TransferOrderId" class="btn btn-sm btn-light">Chi tiáº¿t</a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr><td colspan="7" class="text-center py-4">KhÃ´ng tÃ¬m tháº¥y dá»¯ liá»‡u.</td></tr>
                }
            </tbody>
        </table>
        
        @if(Model.PagedResult.TotalCount > 0)
        {
            <div class="p-3" style="background:#f8fafc; border-top:1px solid #e2e8f0; display:flex; justify-content:space-between;">
                <span>Hiá»ƒn thá»‹ @(((Model.PagedResult.PageIndex - 1) * Model.PagedResult.PageSize) + 1) - @(Math.Min(Model.PagedResult.PageIndex * Model.PagedResult.PageSize, Model.PagedResult.TotalCount)) / @Model.PagedResult.TotalCount</span>
                <div class="d-flex gap-2">
                    <a href="/Transfer/Index?PageIndex=@(Model.PagedResult.PageIndex - 1)&Status=@Model.Status&Keyword=@Model.Keyword" class="btn btn-sm btn-light @(Model.PagedResult.PageIndex <= 1 ? "disabled" : "")">TrÆ°á»›c</a>
                    <a href="/Transfer/Index?PageIndex=@(Model.PagedResult.PageIndex + 1)&Status=@Model.Status&Keyword=@Model.Keyword" class="btn btn-sm btn-light @(Model.PagedResult.PageIndex >= Model.PagedResult.TotalPages ? "disabled" : "")">Sau</a>
                </div>
            </div>
        }
    </div>
</div>

